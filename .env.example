# Продакшен (Render)
VITE_API_URL=https://nutriflow-server.onrender.com/api

# Локальний сервер (розкоментуйте замість рядка вище)
# VITE_API_URL=http://localhost:5000/api

# Якщо змінна не задана — у dev використовується proxy Vite на /api → localhost:5000
